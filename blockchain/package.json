{
    "name": "blockchain-banking-contracts",
    "version": "1.0.0",
    "description": "Smart contracts for the Blockchain Banking system",
    "main": "index.js",
    "scripts": {
        "compile": "hardhat compile",
        "test": "hardhat test",
        "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
        "verify": "hardhat verify"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^3.0.0",
        "hardhat": "^2.17.0",
        "dotenv": "^16.3.1"
    }
}